# Session — Deep-dive node 3: races / write-boundary enforcement (taught to applied-adjacent) + owed recall checks

Date: 2026-09-03
Model: moonshotai/kimi-k3 (verified via PI_MODEL at close)
Goal: (1) owed recall checks (~09-03): node 1 crash window, idempotency, node 2 atomicity; (2) fresh node-2 proof re-run; (3) teach node 3 (races / write-boundary enforcement) grounded in real store.py guarded writes.

## What happened

1. **Node-2 proof re-run (fresh, unaided):** "record exists → what does it prove about the balance, and which mechanism makes the proof trustworthy?" Learner answered clean and unaided: record's existence proves the charge was applied; retry won't recharge; the atomic charge transaction welds the two writes (decrement + usage-record upsert); atomicity is "built to be impossible to lie" — crash mid-transaction → nothing. One sharpening needed: the "calculation" (balance−1) is part of the decrement write, not a separate step in the envelope. Graded: applied-level evidence for node 2's proof chain (the piece that failed at fatigue on 2026-09-01).
2. **Node 3, step 1 (the race):** scenario = naive check-then-act close (SELECT status, then UPDATE), two concurrent requests. Learner first answered one layer up again ("requests faster than they can be executed → fail-fast"), self-flagged ("am I answering the wrong layer again?"). Taught: bug exists with exactly two requests, no load; the check and act are two moments and time passes between them; fix = collapse check-and-act into one indivisible guarded write (atomicity one level down). Grounded in real `mark_conversation_closing` (store.py:156, `WHERE status='OPEN'` + `rowcount != 1` raise). Lock-in: learner initially floundered on guarded-vs-check-then-act internals (self-reported nervousness/blanking on dense questions; re-reads to recover). Retaught with plain version ("set CLOSING **if** currently OPEN; DB runs one write at a time; A wins, B's if is false, B writes nothing"). Learner then got it unaided ("B blocked at the door, doesn't write"). rowcount-guard question: first answer half-right (guard stops B from writing — no, the `if` already did); sharpened: guard stops B from *believing* it won and running completion work a second time. Locked.
3. **Node 3, step 2 (names):** taught race condition / invariant / enforcement-at-the-write-boundary as one idea; answered the original probe gap (durability answer was true but wrong layer). Check: invariant of `reopen_conversation`'s `WHERE status='CLOSING'`. Learner's first answers wrong twice ("can't start again", "only from COMPLETE"), then corrected to "reopen only from CLOSING"; teacher added airtight version: status moves one step at a time along OPEN → CLOSING → COMPLETE, and the write refuses everything else. Locked.
4. **Node 3, step 3 (transfer check):** wallet scenario, check-then-act spend with 3 concurrent requests, 1 free credit. Learner's first answer carried old skin (race "in finding the row"; guard copied from reopen's status condition). Corrected; second try produced the right guard in plain words ("spend a credit *if* free credit is more than 0") and the right rowcount-raise meaning. Graded: transfer achieved with one correction round — learner self-reported "still not confident," which is consistent with first-landing, not with guessing.

## Owed recall checks (all ~5-day delay for node 1, ~2-day for idempotency/node 2)

- Node 1 crash window: recalled unaided, correct shape ("interval in a sequence where a crash → duplication/loss"). Tightened: the span is between commits (durable steps), not everywhere bad things happen. Grade: recallable.
- Idempotency (failed 1-day unaided on 2026-09-01, re-presented inside node 2): recalled unaided this time — "1 run or 50 runs → same final outcome." Grade: recallable (delay ~2 days since re-presentation). First evidence of a re-presentation → delayed recall working.
- Keyed trick (usage record): partially recalled after visible effort (~5-10 min self-reported): recovered most of the flow shape (flag saved so no redo), correctly identified the save as "all-or-nothing kind of write," and — teacher correction later reversed by learner — the real order per application.py `_complete_conversation` is CLOSING → LLM → Discord-send → COMPLETE-as-final-write. Learner's "COMPLETE is last" recollection was CORRECT against teacher's mistaken recap; verified against repo. Teacher error logged. Re-presented: usage record keyed by (conversation, event type), written inside the charge transaction; retry checks existence → skip. Grade: re-derivable, not yet recallable.

## Evidence notes

- Learner self-reports question-density anxiety + blanking on first read, then recovering by re-reading; requests lead-with-the-plain-version ("state whatever the user is trying to learn as SIMPLY as possible"). Logged to learner.md.
- Learner again self-flagged wrong-layer answers unprompted — the 2026-08-26 probe pattern is now a self-correcting habit.
- Node 3 map update: races/write-boundary strand edge → known (explained+, transfer demonstrated with one correction). Full clean applied grade deferred: learner transferred with one correction round, not clean.
- Next: node 4 (state machines) of the frozen deep-dive. Owed recall checks ~2026-09-06: guarded-write definition (race fix), invariant/enforcement-at-write-boundary names, keyed-usage-record trick, node-1 crash window (second interval).
